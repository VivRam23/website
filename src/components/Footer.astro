---
import { site, nav } from '../data/site';
const year = new Date().getFullYear();
---

<footer class="border-t border-grey-200">
  <div class="mx-auto flex max-w-6xl flex-col gap-6 px-6 py-10 sm:flex-row sm:items-center sm:justify-between sm:px-10 lg:px-16">
    <p class="text-sm text-grey-500">© {year} Viviana Rames. All rights reserved.</p>
    <div class="flex flex-wrap items-center gap-x-6 gap-y-2">
      <a href="/" class="text-sm text-grey-700 hover:text-ink">Home</a>
      {nav.map((item) => (
        <a href={item.href} class="text-sm text-grey-700 hover:text-ink">{item.label}</a>
      ))}
      <a href={site.linkedin} class="text-sm text-grey-700 hover:text-ink" rel="me noopener" target="_blank">
        LinkedIn
      </a>
      <a href={`mailto:${site.email}`} class="text-sm text-grey-700 hover:text-ink">
        {site.email}
      </a>
    </div>
  </div>
</footer>
